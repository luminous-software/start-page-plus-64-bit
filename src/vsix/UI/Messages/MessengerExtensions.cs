namespace StartPagePlus.UI.Messages.Extensions.MessengerExtensions
{
    public static class MessengerExtensions
    {
        //public static void ListenFor<TRecipient, TMessage>(this IMessenger messenger, TRecipient recipient, MessageHandler<object, TMessage> action)
        //public static void ListenFor<TMessage>(this IMessenger messenger, MessageHandler<object, TMessage> action)
        //where TMessage : ValueChangedMessage<TMessage>
        //{
        //    messenger.Register(recipient, action);
        //}

        //public static void ListenFor<TRecipient, TMessage>(this IMessenger messenger, IRecipient<TMessage> recipient, MessageHandler<object, TMessage> action)
        //    where TRecipient : class
        //    where TMessage : ValueChangedMessage<TMessage>, new()
        //{
        //    messenger.Register<TMessage>(recipient, action);
        //}

        //Messenger.Register<NewsItemClickedMessage>(this, OnItemClicked);

        //public static void SendMessage(MessageBase message)
        //=> Messenger.Default.Send(message);

    }
}